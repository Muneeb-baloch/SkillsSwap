# Cloudinary (image uploads)
EXPO_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
EXPO_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your_unsigned_upload_preset

# Firebase keys are configured directly in src/config/firebase.js
# (Firebase Web SDK keys are safe to commit — security is enforced via Firestore Rules)
